.panel.panel-primary
   .panel-heading.text-center New Courses
   .panel-body
      .row(ng-repeat="course in courses|orderBy:'published':true|filter:{featured:false}|limitTo:10")
         .col-md-5 {{course.published | date:'dd MMMM, yyyy '}}
         .col-md-7 {{course.name}}